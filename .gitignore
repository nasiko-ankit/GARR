node_modules/
dist/
.env
.env.local
.env.*.local
*.log
coverage/
.DS_Store
.vscode/
.idea/

# Explicit node_modules paths (covers symlinks which trailing-slash pattern misses)
examples/rap-server/node_modules
examples/rap-server-acme/node_modules

# Unzipped agent code — derived from zip files already in repo


# Local-only: internal working docs and source specs
CLAUDE.md
PROGRESS.md
*.docx

# Demo private keys — never commit
.keys.demo.json

