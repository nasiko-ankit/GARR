node_modules/
dist/
.env
.env.local
.env.*.local
*.log
coverage/
.DS_Store
.vscode/
.idea/

# Local-only: internal working docs and source specs
CLAUDE.md
PROGRESS.md
*.docx

# Cryptographic material — NEVER commit. Generate locally per README.
*.pem
*.key

# Demo seed artifacts: scripts/seed-demo.mjs generates these locally
# and they bake in the running server's port, so they're per-machine
# rather than committed. Re-run the seed after starting the dev server.
db/seed/keys/
db/seed/*.json
.env.demo
