node_modules/
dist/
.env
.env.local
.env.*.local
*.log
coverage/
.DS_Store
.vscode/
.idea/

# Local-only: internal working docs and source specs
CLAUDE.md
PROGRESS.md
*.docx

# Cryptographic material — NEVER commit. Generate locally per README.
*.pem
*.key

# Demo seed artifacts: private keys live here, generated by
# scripts/seed-demo.mjs. The signed JSON files (db/seed/*.json) are
# committed; only the keys/ subdir + per-run env are private.
db/seed/keys/
.env.demo
